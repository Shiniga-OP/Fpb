#incluir "biblis/impressao.asm";
#global inicio();

vazio inicio() {
    bool x = falso;
    bool y = falso;
    bool z = falso;
    enq(!x && !y && !z) {
        escrever("Teste, X = ", x, "\n");
        x = verdade;
        y = verdade;
        z = verdade;
    }
}