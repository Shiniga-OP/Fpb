#incluir "biblis/mem.asm";
#incluir "biblis/texs.asm";
#global inicio();

vazio memcp(car[] arr, car* p, int tam);
int textam(car* p);

vazio inicio() {
    car[] arr = "exemplo";
    car* p = "XxXemplo";
    int tam = textam(arr);
    memcp(arr, p, tam);
}
